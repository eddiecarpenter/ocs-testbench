name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: eddiecarpenter/gh-agentic/.github/workflows/release.yml@v2.5.3
    secrets: inherit
    permissions:
      contents: write
