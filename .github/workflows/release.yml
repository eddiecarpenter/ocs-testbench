name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: eddiecarpenter/gh-agentic/.github/workflows/release.yml@v2.3.0
    secrets: inherit
    permissions:
      contents: write
