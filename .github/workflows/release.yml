name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: eddiecarpenter/gh-agentic/.github/workflows/release-reusable.yml@v2.2.5
    secrets: inherit
    permissions:
      contents: write
