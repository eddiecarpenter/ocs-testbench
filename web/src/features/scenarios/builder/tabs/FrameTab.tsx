import { Text } from '@mantine/core';

/**
 * Builder — Frame tab. Placeholder until Task 5 lands the AVP tree,
 * properties pane, and the engine-managed dimming.
 */
export function FrameTab() {
  return <Text c="dimmed">Frame tab — implementation lands in Task 5.</Text>;
}
