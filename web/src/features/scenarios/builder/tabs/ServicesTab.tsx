import { Text } from '@mantine/core';

/**
 * Builder — Services tab. Placeholder until Task 6 lands the segmented
 * `serviceModel × unitType` matrix and the per-service editor.
 */
export function ServicesTab() {
  return <Text c="dimmed">Services tab — implementation lands in Task 6.</Text>;
}
