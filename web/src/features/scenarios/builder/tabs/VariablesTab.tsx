import { Text } from '@mantine/core';

/**
 * Builder — Variables tab. Placeholder until Task 7 lands the System /
 * User sidebar, definition pane, and Usage backlinks.
 */
export function VariablesTab() {
  return (
    <Text c="dimmed">Variables tab — implementation lands in Task 7.</Text>
  );
}
