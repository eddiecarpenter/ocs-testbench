// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: peer.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deletePeer = `-- name: DeletePeer :exec
DELETE FROM peer
WHERE id = $1
`

func (q *Queries) DeletePeer(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deletePeer, id)
	return err
}

const getPeerByID = `-- name: GetPeerByID :one
SELECT id, name, body, created_at, updated_at
FROM peer
WHERE id = $1
`

func (q *Queries) GetPeerByID(ctx context.Context, id pgtype.UUID) (Peer, error) {
	row := q.db.QueryRow(ctx, getPeerByID, id)
	var i Peer
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Body,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getPeerByName = `-- name: GetPeerByName :one
SELECT id, name, body, created_at, updated_at
FROM peer
WHERE name = $1
`

func (q *Queries) GetPeerByName(ctx context.Context, name string) (Peer, error) {
	row := q.db.QueryRow(ctx, getPeerByName, name)
	var i Peer
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Body,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertPeer = `-- name: InsertPeer :one

INSERT INTO peer (name, body)
VALUES ($1, $2)
RETURNING id, name, body, created_at, updated_at
`

// Queries for the peer table.
//
// Avoids Postgres-specific operators per the data-model Feature
// (no @>, no array predicates) so the SQL stays portable to a
// future SQLite engine swap.
// Inserts a peer; the id is generated server-side via the schema's
// gen_random_uuid() default. Returns the inserted row so the caller
// can persist the resulting id without a follow-up read.
func (q *Queries) InsertPeer(ctx context.Context, name string, body []byte) (Peer, error) {
	row := q.db.QueryRow(ctx, insertPeer, name, body)
	var i Peer
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Body,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listPeers = `-- name: ListPeers :many
SELECT id, name, body, created_at, updated_at
FROM peer
ORDER BY name
`

func (q *Queries) ListPeers(ctx context.Context) ([]Peer, error) {
	rows, err := q.db.Query(ctx, listPeers)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Peer{}
	for rows.Next() {
		var i Peer
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updatePeer = `-- name: UpdatePeer :one
UPDATE peer
SET name = $2,
    body = $3,
    updated_at = now()
WHERE id = $1
RETURNING id, name, body, created_at, updated_at
`

// updated_at is bumped in SQL so callers cannot accidentally skip it.
// Per design plan the store layer (not a trigger) maintains updated_at
// to keep the schema portable to SQLite.
func (q *Queries) UpdatePeer(ctx context.Context, iD pgtype.UUID, name string, body []byte) (Peer, error) {
	row := q.db.QueryRow(ctx, updatePeer, iD, name, body)
	var i Peer
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Body,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
