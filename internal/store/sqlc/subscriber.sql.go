// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: subscriber.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deleteSubscriber = `-- name: DeleteSubscriber :exec
DELETE FROM subscriber
WHERE id = $1
`

func (q *Queries) DeleteSubscriber(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteSubscriber, id)
	return err
}

const getSubscriberByID = `-- name: GetSubscriberByID :one
SELECT id, name, msisdn, iccid, imei, created_at, updated_at, tac
FROM subscriber
WHERE id = $1
`

func (q *Queries) GetSubscriberByID(ctx context.Context, id pgtype.UUID) (Subscriber, error) {
	row := q.db.QueryRow(ctx, getSubscriberByID, id)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Msisdn,
		&i.Iccid,
		&i.Imei,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Tac,
	)
	return i, err
}

const getSubscriberByName = `-- name: GetSubscriberByName :one
SELECT id, name, msisdn, iccid, imei, created_at, updated_at, tac
FROM subscriber
WHERE name = $1
ORDER BY id
LIMIT 1
`

// Subscribers do not have a unique name constraint at the schema
// level, so a "by name" lookup may match multiple rows; the store
// layer chooses the deterministic first match (alphabetical id) for
// catalogue UX. Tests that need richer semantics should use
// ListSubscribers and filter on the result.
func (q *Queries) GetSubscriberByName(ctx context.Context, name string) (Subscriber, error) {
	row := q.db.QueryRow(ctx, getSubscriberByName, name)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Msisdn,
		&i.Iccid,
		&i.Imei,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Tac,
	)
	return i, err
}

const insertSubscriber = `-- name: InsertSubscriber :one

INSERT INTO subscriber (
    name,
    msisdn,
    iccid,
    imei,
    tac
)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, name, msisdn, iccid, imei, created_at, updated_at, tac
`

type InsertSubscriberParams struct {
	Name   string
	Msisdn string
	Iccid  string
	Imei   pgtype.Text
	Tac    pgtype.Text
}

// Queries for the subscriber table.
//
// Insert and Update declare more than four parameters; with
// query_parameter_limit: 4 in sqlc.yaml the generated bindings will
// accept an `Arg` struct rather than positional parameters, which
// keeps the call sites readable.
func (q *Queries) InsertSubscriber(ctx context.Context, arg InsertSubscriberParams) (Subscriber, error) {
	row := q.db.QueryRow(ctx, insertSubscriber,
		arg.Name,
		arg.Msisdn,
		arg.Iccid,
		arg.Imei,
		arg.Tac,
	)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Msisdn,
		&i.Iccid,
		&i.Imei,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Tac,
	)
	return i, err
}

const listSubscribers = `-- name: ListSubscribers :many
SELECT id, name, msisdn, iccid, imei, created_at, updated_at, tac
FROM subscriber
ORDER BY name
`

func (q *Queries) ListSubscribers(ctx context.Context) ([]Subscriber, error) {
	rows, err := q.db.Query(ctx, listSubscribers)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Subscriber{}
	for rows.Next() {
		var i Subscriber
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Msisdn,
			&i.Iccid,
			&i.Imei,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Tac,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSubscriber = `-- name: UpdateSubscriber :one
UPDATE subscriber
SET name       = $2,
    msisdn     = $3,
    iccid      = $4,
    imei       = $5,
    tac        = $6,
    updated_at = now()
WHERE id = $1
RETURNING id, name, msisdn, iccid, imei, created_at, updated_at, tac
`

type UpdateSubscriberParams struct {
	ID     pgtype.UUID
	Name   string
	Msisdn string
	Iccid  string
	Imei   pgtype.Text
	Tac    pgtype.Text
}

func (q *Queries) UpdateSubscriber(ctx context.Context, arg UpdateSubscriberParams) (Subscriber, error) {
	row := q.db.QueryRow(ctx, updateSubscriber,
		arg.ID,
		arg.Name,
		arg.Msisdn,
		arg.Iccid,
		arg.Imei,
		arg.Tac,
	)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Msisdn,
		&i.Iccid,
		&i.Imei,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Tac,
	)
	return i, err
}
