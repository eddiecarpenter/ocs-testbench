// Package baseconfig holds the application's runtime configuration types.
//
// It will expose the YAML-backed configuration surface (database URL,
// Diameter peer settings, API listener, etc.) once the surrounding
// Features land. At present the package exists so the module skeleton
// compiles; no public API has been defined yet.
package baseconfig
