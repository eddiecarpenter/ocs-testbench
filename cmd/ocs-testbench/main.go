// Command ocs-testbench is the entry point for the OCS Testbench application.
//
// At this stage of the build only the package shell exists. Wiring of
// configuration, logging, the persistence layer, the Diameter stack, and
// the API server is delivered by later Features.
package main

func main() {}
